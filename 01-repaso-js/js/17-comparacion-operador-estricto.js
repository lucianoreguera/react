const n1 = 20
const n2 = "20"

// == Comparación no estricta - compara valor pero no tipo
// === Comparación estricta - compara valor y tipo
// Sugerencias usar el ===

if (n1 === n2) {
    console.log('Si!, si son iguales')
} else {
    console.log('No!, no son iguales')
}